# Proximity-Assignment

A simple web app that displays the current stock values using WebSockets. 

In order to run, type npm install to install packages.
Type npm start to run the app on localhost 3000
